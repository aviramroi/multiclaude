---
description: Print the share link a teammate needs to clone this session
allowed-tools: Bash(mc:*)
---
Run `mc share` (pushing first with `mc push --link` if the session is not tracked yet) and show the link.
