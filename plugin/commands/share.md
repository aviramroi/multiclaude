---
description: Show the share link for this session (runs mc share inline, no reasoning needed)
allowed-tools: Bash(mc:*)
---
Share link (pushed just now):

!`mc push --link >/dev/null 2>&1; mc share`

Reply with exactly the line above and nothing else.
