---
description: Clone a shared session (from a teammate's share link) into this project
allowed-tools: Bash(mc:*)
argument-hint: "<share-url> "
---
Run `mc clone $ARGUMENTS`. Print the `claude --resume <id>` command it returns and offer to
summarize the cloned transcript with `mc log <id>`.
